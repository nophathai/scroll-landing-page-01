# Navbar0A

Framer Code Component for the portfolio navbar.

Use `Navbar0A.tsx` as a Framer code component. Upload/select `Logo.png` in the Logo property if Framer does not resolve the local default path.

Project links open in the same tab. Resume opens in a new tab.
